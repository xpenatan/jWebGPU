/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/
package com.github.xpenatan.webgpu;

import java.util.Map;
import java.util.HashMap;
import com.github.xpenatan.jParser.idl.IDLEnum;

public enum WGPUByteOrder implements IDLEnum<WGPUByteOrder> {

    CUSTOM(0), BigEndian(BigEndian_NATIVE()), LittleEndian(LittleEndian_NATIVE());

    private int value;

    private WGPUByteOrder(int value) {
        this.value = value;
    }

    public int getValue() {
        return value;
    }

    public WGPUByteOrder setValue(int value) {
        if (this != CUSTOM) {
            throw new RuntimeException("Cannot change none CUSTOM value");
        }
        this.value = value;
        return this;
    }

    public WGPUByteOrder getCustom() {
        return CUSTOM;
    }

    public static final Map<Integer, WGPUByteOrder> MAP = new HashMap<>();

    static {
        for (WGPUByteOrder value : values()) {
            if (value != CUSTOM) {
                MAP.put(value.value, value);
            }
        }
    }

    /*[-JNI;-NATIVE]
return (jlong)BigEndian;
*/
    private static native int BigEndian_NATIVE();

    /*[-JNI;-NATIVE]
return (jlong)LittleEndian;
*/
    private static native int LittleEndian_NATIVE();
}
