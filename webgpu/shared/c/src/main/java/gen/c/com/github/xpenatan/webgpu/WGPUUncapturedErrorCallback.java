/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.c.com.github.xpenatan.webgpu;

import com.github.xpenatan.jParser.api.NativeObject;

public class WGPUUncapturedErrorCallback extends NativeObject {

    private static final java.util.ArrayList<WGPUUncapturedErrorCallback> TEAVMC_CALLBACKS = new java.util.ArrayList<>();

    private int teavmcCallbackId = -1;

    private int teavmcRegisterCallback() {
        if (teavmcCallbackId < 0) {
            teavmcCallbackId = TEAVMC_CALLBACKS.size();
            TEAVMC_CALLBACKS.add(this);
        } else {
            TEAVMC_CALLBACKS.set(teavmcCallbackId, this);
        }
        return teavmcCallbackId;
    }

    private static String teavmcCStringToString(org.teavm.interop.Address address) {
        if (address == null || address.toLong() == 0) {
            return null;
        }
        int length = 0;
        while (address.add(length).getByte() != 0) {
            length++;
        }
        char[] chars = new char[length];
        for (int i = 0; i < length; i++) {
            chars[i] = (char) (address.add(i).getByte() & 0xFF);
        }
        return new String(chars);
    }

    private static abstract class TEAVMC_OnCallback_Function extends org.teavm.interop.Function {

        public abstract void call(int callbackId, int errorType_addr, org.teavm.interop.Address message_addr);
    }

    @org.teavm.interop.Export(name = "teavmc_WGPUUncapturedErrorCallback_OnCallback")
    private static void teavmc_WGPUUncapturedErrorCallback_OnCallback(int callbackId, int errorType_addr, org.teavm.interop.Address message_addr) {
        TEAVMC_CALLBACKS.get(callbackId).internal_OnCallback(errorType_addr, teavmcCStringToString(message_addr));
    }

    @org.teavm.interop.Import(name = "teavmc_WGPUUncapturedErrorCallback_setupCallback")
    private static native void setupCallback(long this_addr, int callbackId, TEAVMC_OnCallback_Function OnCallback_fp);

    static public final WGPUUncapturedErrorCallback NULL = WGPUUncapturedErrorCallback.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected WGPUUncapturedErrorCallback(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static WGPUUncapturedErrorCallback native_new() {
        return new WGPUUncapturedErrorCallback((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.interop.Import(name = "n2433197308")
    public static native void internal_native_deleteNative(long this_addr);

    public WGPUUncapturedErrorCallback() {
        long addr = internal_native_create_addr();
        internal_reset(addr, true);
        setupCallback();
    }

    private void setupCallback() {
        int callbackId = teavmcRegisterCallback();
        setupCallback(native_address, callbackId, org.teavm.interop.Function.get(TEAVMC_OnCallback_Function.class, WGPUUncapturedErrorCallback.class, "teavmc_WGPUUncapturedErrorCallback_OnCallback"));
    }

    protected void onCallback(WGPUErrorType errorType, String message) {
    }

    private void internal_OnCallback(int errorType_addr, String message_addr) {
        WGPUErrorType errorType_addr_enum = WGPUErrorType.CUSTOM.setValue(errorType_addr);
        WGPUErrorType[] errorType_addr_enum_values = WGPUErrorType.values();
        for (int i = 0; i < errorType_addr_enum_values.length; i++) {
            WGPUErrorType enumVal = errorType_addr_enum_values[i];
            if (enumVal != WGPUErrorType.CUSTOM && enumVal.getValue() == errorType_addr) {
                errorType_addr_enum = errorType_addr_enum_values[i];
                break;
            }
        }
        onCallback(errorType_addr_enum, message_addr);
    }

    @org.teavm.interop.Import(name = "n101496194")
    public static native long internal_native_create_addr();
}
