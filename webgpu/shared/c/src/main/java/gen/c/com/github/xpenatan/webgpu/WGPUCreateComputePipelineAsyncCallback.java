/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.c.com.github.xpenatan.webgpu;

import com.github.xpenatan.jParser.api.NativeObject;

public class WGPUCreateComputePipelineAsyncCallback extends NativeObject {

    private static final java.util.ArrayList<WGPUCreateComputePipelineAsyncCallback> TEAVMC_CALLBACKS = new java.util.ArrayList<>();

    private int teavmcCallbackId = -1;

    private int teavmcRegisterCallback() {
        if (teavmcCallbackId < 0) {
            teavmcCallbackId = TEAVMC_CALLBACKS.size();
            TEAVMC_CALLBACKS.add(this);
        } else {
            TEAVMC_CALLBACKS.set(teavmcCallbackId, this);
        }
        return teavmcCallbackId;
    }

    private static String teavmcCStringToString(org.teavm.interop.Address address) {
        if (address == null || address.toLong() == 0) {
            return null;
        }
        int length = 0;
        while (address.add(length).getByte() != 0) {
            length++;
        }
        char[] chars = new char[length];
        for (int i = 0; i < length; i++) {
            chars[i] = (char) (address.add(i).getByte() & 0xFF);
        }
        return new String(chars);
    }

    private static abstract class TEAVMC_OnCallback_Function extends org.teavm.interop.Function {

        public abstract void call(int callbackId, int status_addr, long pipeline_addr, org.teavm.interop.Address message_addr);
    }

    @org.teavm.interop.Export(name = "teavmc_WGPUCreateComputePipelineAsyncCallback_OnCallback")
    private static void teavmc_WGPUCreateComputePipelineAsyncCallback_OnCallback(int callbackId, int status_addr, long pipeline_addr, org.teavm.interop.Address message_addr) {
        TEAVMC_CALLBACKS.get(callbackId).internal_OnCallback(status_addr, pipeline_addr, teavmcCStringToString(message_addr));
    }

    @org.teavm.interop.Import(name = "teavmc_WGPUCreateComputePipelineAsyncCallback_setupCallback")
    private static native void setupCallback(long this_addr, int callbackId, TEAVMC_OnCallback_Function OnCallback_fp);

    static public final WGPUCreateComputePipelineAsyncCallback NULL = WGPUCreateComputePipelineAsyncCallback.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected WGPUCreateComputePipelineAsyncCallback(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static WGPUCreateComputePipelineAsyncCallback native_new() {
        return new WGPUCreateComputePipelineAsyncCallback((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.interop.Import(name = "n1149141350")
    public static native void internal_native_deleteNative(long this_addr);

    public WGPUCreateComputePipelineAsyncCallback() {
        long addr = internal_native_create_addr();
        internal_reset(addr, true);
        setupCallback();
    }

    private void setupCallback() {
        int callbackId = teavmcRegisterCallback();
        setupCallback(native_address, callbackId, org.teavm.interop.Function.get(TEAVMC_OnCallback_Function.class, WGPUCreateComputePipelineAsyncCallback.class, "teavmc_WGPUCreateComputePipelineAsyncCallback_OnCallback"));
    }

    protected void onCallback(WGPUCreatePipelineAsyncStatus status, WGPUComputePipeline pipeline, String message) {
    }

    private void internal_OnCallback(int status_addr, long pipeline_addr, String message_addr) {
        WGPUCreatePipelineAsyncStatus status_addr_enum = WGPUCreatePipelineAsyncStatus.CUSTOM.setValue(status_addr);
        WGPUCreatePipelineAsyncStatus[] status_addr_enum_values = WGPUCreatePipelineAsyncStatus.values();
        for (int i = 0; i < status_addr_enum_values.length; i++) {
            WGPUCreatePipelineAsyncStatus enumVal = status_addr_enum_values[i];
            if (enumVal != WGPUCreatePipelineAsyncStatus.CUSTOM && enumVal.getValue() == status_addr) {
                status_addr_enum = status_addr_enum_values[i];
                break;
            }
        }
        WGPUComputePipeline pipeline_addr_new = WGPUComputePipeline.native_new();
        pipeline_addr_new.internal_reset(pipeline_addr, true);
        onCallback(status_addr_enum, pipeline_addr_new, message_addr);
    }

    @org.teavm.interop.Import(name = "n2477014744")
    public static native long internal_native_create_addr();
}
